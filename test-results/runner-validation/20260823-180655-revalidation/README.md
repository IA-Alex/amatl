# Offline benchmark-runner validation

Generated offline benchmark-runner validation artifact — no provider execution performed.

This artifact contains immutable plan validation only. AMATL, providers, HTTP, DNS and sockets were not invoked.

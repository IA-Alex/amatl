# Diagnóstico local del controlador

Generated static controller/persistence diagnostic — no AMATL or provider execution performed.

Scope: inspección estática del runner y de los artefactos locales. No se modificó el directorio histórico `test-results/benchmarks/searxng-v2/20260823-190535/`.

El estado histórico disponible es internamente inconsistente con la descripción de la campaña: `runs.jsonl`, `metrics.json` y `execution-plan.json` describen 30 ejecuciones, mientras que el estado conocido declara 9 reportadas y 0 durables. Por ello no se atribuye causalidad no demostrable.

Generated test artifact — SearXNG per-engine runtime observation — do not treat as project documentation.

One AMATL invocation of `rust async` was made. It failed before SearXNG accepted a request, so the required per-engine observation is blocked and no retry was made.

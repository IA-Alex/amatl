Generated test artifact — SearXNG per-engine runtime observation — do not treat as project documentation.

The sole normal AMATL `rust async` invocation did not reach SearXNG: AMATL reported `provider_unavailable` for SearXNG and the temporary SearXNG probe emitted no scheduler or processor event. Marginalia is recorded only as its public `provider_rate_limit` status. No second query was made.

The two temporary module modifications were restored. Their final SHA-256 values equal the original hashes. Docker's filesystem diff retains historical change markers and cache files produced by container startup, but the hashes prove neither instrumented source file remains modified.
